# Whisper Subtitle Generator Configuration

# Secret key for JWT tokens (change in production!)
SECRET_KEY=change-me-in-production-use-a-long-random-string

# Default Whisper model (tiny, base, small, medium, large-v2, large-v3, large-v3-turbo, large-v3-int4, large-v3-int8, large-v3-fp16)
DEFAULT_MODEL=large-v3-int8

# Number of CPU threads to use (0 = auto-detect)
CPU_THREADS=4
